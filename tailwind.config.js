/** @type {import('tailwindcss').Config} */
export default {
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
  theme: {
    extend: {
      colors: {
        // 색상 정의
      },
      height: {
        // 높이 정의 ,
      },
      width: {
        // 너비 정의
      },
    },
    fontFamily: {
      Pretendard: ['Pretendard'],
    },
  },
};
